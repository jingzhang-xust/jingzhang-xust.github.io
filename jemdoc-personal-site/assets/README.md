# Assets

`profile.svg` is only a placeholder.

When you are ready, replace it with your real academic photo. You can either:

1. overwrite `profile.svg` with your own SVG, or
2. add `profile.jpg` / `profile.png` and change the image path in `index.jemdoc`.

Recommended portrait ratio: roughly 4:5 or 3:4.
